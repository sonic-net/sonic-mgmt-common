# This repo contains all public API declarations for SDI

# Description
Has all public API declarations.  API are kept in a seperate repository to facilitate reuse if these with diff arch implementation


